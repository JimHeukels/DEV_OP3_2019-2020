class Program {
  public static void Main() {
    string   greet;
    string   who;
    string   s;
    greet = "Hello";
    who = "world";
    s = greet + " " + who + "!";

    string task = "Swap the values in the vaiables";
    string temp;
    temp = who;
    who = greet;
    greet = temp;
    
  }
}